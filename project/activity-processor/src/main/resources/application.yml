spring:

  rabbitmq:
    addresses: rabbitmq
    
  datasource:
    driverClassName: ${DB_DRIVER}      
    url: ${DB_URL}
    user: ${DB_USER}
                                   
  jpa:
      hibernate:
         ddl-auto: none  

management:
  endpoints:
    web:
      exposure:
        include: health, info, userrequeststat
  endpoint:
    health:
      show-details: always

logging:
  file: log.txt